services:
  - type: web
    name: opay-insight-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python -m uvicorn api.app:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    envVars:
      - key: MIN_FEATURE_TRIM_ENABLED
        value: "1"
